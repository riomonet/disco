import React, { useState } from 'react';

// Each tile set a random color at a random interval;

function Tile () {

    const getInterval  = () => {}
    const getColor = () => {}
    
    const [interval, setInterval] = useState(getInterval());
    const [color, setColor]  = useState(0); //can color be some kind of rgb value

    return <div className="Tile">

	   </div>

}
